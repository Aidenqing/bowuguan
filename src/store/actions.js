import * as types from './mutation-types'
// flg1 表示是否单选，flg2表示是否选中
/*
export const setVideoStatus = function ({commit, state}, { index, flg1, flg2}) { // 设置预设公司名称
  let vlist = state.videoList.slice()
  if (flg1) {
    for (let item in vlist) {
      item.status = false
    }
  }
  vlist[index].status = flg2
  commit(types.SET_VIDEOLIST, vlist)
}
*/
